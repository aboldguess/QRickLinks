# QRickLinks Environment Template
#
# This file documents the configuration variables recognised by the
# application.  Copy it to ``.env`` and adjust the values for your deployment.
# Secrets should never be committed to version control; storing them in ``.env``
# keeps them local to your machine while ``python-dotenv`` loads them
# automatically.

# Flask session secret.  Use ``python -c "import secrets; print(secrets.token_hex(32))"``
# to generate a unique value before running the app in production.
SECRET_KEY=change-this-secret-key

# Database connection string.  Leave as SQLite for local development or supply a
# full SQLAlchemy URI, e.g. ``postgresql+psycopg://user:password@host/dbname``.
SQLALCHEMY_DATABASE_URI=sqlite:///qricklinks.db

# Adjust the log level for server output.  Valid values: DEBUG, INFO, WARNING,
# ERROR, CRITICAL.
QRICKLINKS_LOG_LEVEL=INFO

# Optional OAuth credentials for Google login.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# API key for the thum.io preview service if thumbnails are enabled.  Leave
# blank to skip authenticated requests.
THUMIO_API_KEY=
